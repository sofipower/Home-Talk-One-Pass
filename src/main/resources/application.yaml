server:
  servlet:
    context-path: /hometop

spring:
  application:
    name: onepass

  profiles:
    active: dev

  jpa:
    open-in-view: false  #캐시 안남도록 false 설정
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    encoding: UTF-8